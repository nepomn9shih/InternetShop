import * as R from 'ramda'

export const getPhoneById = (state, id) => R.prop(id, state.phones)

export const getPhones = state => {
  const phones = R.map(id => getPhoneById(state, id), state.phonesPage.ids)

  return phones
}

export const getRenderedPhonesLength = state => R.length(state.phonesPage.ids)

export const getTotalBasketCount = state => R.length(state.basket)

export const getTotalBasketPrice = state => {
  const totalPrice = R.compose(
    R.sum,                               //3. Суммируем все элементы массива цен
    R.pluck('price'),                    //2. Извлекаем из массива массив цен
    R.map(id => getPhoneById(state, id)) //1. Из массива id делаем массив смартфонов с нужным id
  )(state.basket)
  return totalPrice
}